@model IEnumerable<Book>
<h1>Books</h1>
<table>
    <tr>
        <th>Title</th>
        <th>Genre</th>
    </tr>
    @foreach (var book in Model)
    {
        <tr>
            <td>@book.Title</td>
            <td>@book.Genre.Name</td>
        </tr>
    }
</table>
